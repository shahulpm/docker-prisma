{
  "name": "prisma-docker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:prod": "npx prisma migrate deploy && npm run seed && node app.js",
    "seed": "node seed.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^5.16.2"
  },
  "dependencies": {
    "@prisma/client": "5.16.2",
    "express": "^4.19.2"
  }
}